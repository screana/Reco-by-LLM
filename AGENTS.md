# Reco-by-LLM コーディング & プロジェクト運用ルール

## 概要
プロジェクト内でコードを書いたり修正する際、以下のルールに従ってください。  
---

## 1. コードスタイル

### フォーマット
- インデントは **スペース 4** を使用します。  
- 行の長さは **80〜100 文字以内** を推奨します。  
- 行末に不要なスペースを残さないでください。  
- 空行は必要最小限にし、統一的に使います。 :contentReference[oaicite:1]{index=1}

### 命名規則
- 変数・属性・関数：**snake_case** を基本とします。  
- クラス名：**PascalCase** を使用します。  
- 定数：**SCREAMING_SNAKE_CASE** を使用します。  
- API 用の JSON キー名は camelCase ではなく snake_case を推奨します。 

### 命名に関する補足
- 意味のある名前を付けてください（何をするかが即座にわかる）。  
- テスト用コードにも同様の命名規則を適用します。

---

## 2. コメント & ドキュメント

- コメントは **「なぜやるのか」** を説明するために使い、**「何をやっているか説明するためではない」**ことを原則にします。  
- 関数やクラスには docstring を入れ、引数・返り値・例外について必ず記述します。  
- README にはプロジェクト構造・セットアップ手順・必須コマンドを記載します。 

---

## 3. JSON & API 仕様

- 推薦システムのタスクルーティング API は、**JSON のみを標準応答形式**とします。  
- JSON キー名・値の型は常に仕様書（docs/json_spec.md 等）に従い変更しないでください。  
- エラー時は `{"error": "...", "code": "..."}` の形式で返します。

---

## 4. テスト & CI

- テストは必ず書き、**pytest** などのフレームワークを標準とします。  
- カバレッジは **80%以上** を目標にします。  
- CI では lint, test, build を必ず実行します（pull request 前に必須）。 

---

## 5. コード品質 & 原則

### SOLID 原則（必要に応じて）
- **SRP**: 1 つのモジュール/クラス/関数は **1 つの責務のみ**。  
- **OCP**: 拡張には開かれ、修正には閉じる設計。  
- **LSP / ISP / DIP** も可能な範囲で遵守します。

### 不要な複雑さを避ける
- **KISS**（Keep It Simple, Stupid）、**YAGNI**（You Aren’t Gonna Need It）を念頭に置きます。 

---

## 6. ブランチ & Git 運用

- ブランチ命名は `feature/...`, `fix/...`, `hotfix/...` を統一して用います。  
- コミットメッセージは **簡潔かつ意味ある文** とし、必ず問題チケットへのリンクを記載します。

---

## 7. セキュリティと依存関係

- 外部 API キー・秘密情報は **決してコミットしない**こと。  
- 依存ライブラリは定期的に更新し、脆弱性チェックを CI に含めます。

---